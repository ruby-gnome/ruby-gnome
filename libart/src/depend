install-so:
	$(INSTALL_DATA) $(srcdir)/rbart.h $(RUBYARCHDIR)

install:
	if test -n "$(pkgconfigdir)"; then			\
	  $(MAKEDIRS) $(pkgconfigdir);				\
	  $(INSTALL_DATA) ../ruby-libart2.pc $(pkgconfigdir);	\
	fi
