install-so:
	$(INSTALL_DATA) $(srcdir)/rbpango.h $(RUBYARCHDIR)
	$(INSTALL_DATA) rbpangoversion.h $(RUBYARCHDIR)

install:
	$(MAKEDIRS) $(libdir)/pkgconfig
	$(INSTALL_DATA) ../ruby-pango.pc $(libdir)/pkgconfig
